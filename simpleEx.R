# Define a variable
c <- rnorm(10)

# calculate thr mean of x an print out the results
mux = mean(x)
cat("The mean of x is ", mean(x), "\n")

#print out a summary of the results
summary(x)
cat("The summary of x is \n", summary(x), "\n")
print(summary(x))
